# System/build dependencies for backend/

# Build tools
cmake
g++ (C++17)
pkg-config

# C++ libs
drogon
jsoncpp (dev)
ROOT (with RHTTP)
Boost (only if C++14 toolchain)
libpqxx (PostgreSQL C++ client)
postgresql-client-dev (libpq headers/libs)

# Oracle Instant Client (manual install)
oracle-instantclient (libclntsh, libclntshcore, libnnz + SDK headers)

# Python and python embedding
python3-dev (Python.h)

# Python ML training (backend/function_prediction)
python3
pip
torch
torchvision
torch-geometric
numpy
pandas
matplotlib
pyarrow
uproot
tqdm
pympler
scipy
networkx
pyroot (ROOT python bindings)

# Onnx Runtime (manual install)
onnxruntime (C++ headers + libonnxruntime.so)
